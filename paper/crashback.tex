\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{float}  % [H] pins figures/tables in place so they never drift past their section
\usepackage{hyperref}

\title{Predicting Short-Term Recovery After Large Stock-Price Crashes}
\author{Studio R42}
\date{\today}

\begin{document}
\maketitle

\section{Motivation}

After a large single-day decline, some stocks rebound within weeks while others
keep falling. We ask a single question: at the moment a stock crashes, using only
information available at that time, can we predict \emph{which} stocks recover, by
how much, and how quickly? A crash is defined as a daily adjusted return of
$-10\%$ or worse, and every qualifying day is treated as its own prediction event
(no cooldown or future-dependent suppression), which reproduces the information set
a forecaster would actually face in real time.

The underlying hypothesis is that a crash reflects one of two very different things:
temporary, overreaction-driven price damage, or genuine long-term fundamental
damage. If the two are separable from contemporaneous information, then recovery
should be predictable above its base rate. We therefore frame the problem as
\emph{calibrated probability prediction} rather than as a trading system: the
priority is a defensible, leakage-free historical dataset, honest base rates, and a
measurement of whether successive information sources---price behavior, market and
sector context, point-in-time fundamentals, and later contemporaneous
filings---add predictive value. Concretely, the primary target is the probability
that a stock's close reaches at least $+10\%$ above its crash-day close at some
point within the next $20$ trading days.

\section{Data}

All market data come from Wharton Research Data Services (WRDS): CRSP for
securities, prices, and delistings, and Compustat for fundamentals. To avoid
survivorship bias, the universe is built from the full historical cross-section of
US-listed common stock---including securities that were later delisted, acquired,
renamed, or went bankrupt---rather than from today's survivors. We restrict to
common shares on NYSE, NYSE American, and Nasdaq and exclude ETFs, funds, ADRs,
warrants, and preferred shares.

We use the CRSP CIZ tables: daily prices (\texttt{dsf\_v2}), the historical security
master with full ticker history (\texttt{stocknames\_v2}), and delisting records
(\texttt{dsedelist}). Crash detection uses total returns (split- and
dividend-adjusted) so that corporate actions cannot manufacture false crashes, and
delisting returns are preserved so that terminal outcomes (e.g.\ bankruptcies) are
recorded rather than silently dropped. Fundamentals come from Compustat's unrestated
quarterly data (\texttt{comp\_urq}), dated by \emph{public availability} (the report
date, \texttt{rdq}) rather than by fiscal period end, so a quarter's figures only
become a feature after they were actually released. CRSP and Compustat are linked
through \texttt{ccmxpf\_lnkhist} (\texttt{permno}$\leftrightarrow$\texttt{gvkey}). For
the V2 extension, contemporaneous SEC filings are retrieved from EDGAR and keyed by
each filing's exact acceptance timestamp, so that only documents public by the
crash-day close are admissible.

Every feature is required to be knowable by the prediction timestamp (the crash-day
close), and provider-specific fields are normalized into canonical schemas as early
as possible so that downstream stages depend only on the schema, not on the vendor.
Table~\ref{tab:data} summarizes the resulting dataset.

\begin{table}[H]
\centering
\begin{tabular}{lr}
\toprule
Quantity & Value \\
\midrule
Securities (survivorship-safe) & 27{,}363 \\
\quad active / delisted & 4{,}171 / 23{,}192 \\
Daily price observations & $\sim$85.3 million \\
Coverage & 1925--2025 \\
Crash events (daily return $\le -10\%$) & 1{,}153{,}414 \\
\quad in liquid study pool & $\sim$268{,}000 \\
\bottomrule
\end{tabular}
\caption{Dataset summary. The liquid study pool applies point-in-time universe and
minimum-price filters computed only from information available at the event.}
\label{tab:data}
\end{table}

\section{Model 0: The Base Rate}
\label{sec:m0}

Before using any features, we establish the unconditional benchmark: ignore
everything about the crash and predict the same recovery probability for every event.
This is Model 0, and it is the bar that any feature-based model must clear. For this
baseline we adopt the plainest notion of recovery---\emph{earning money one year
later}---defined as a positive total holding return over the $252$ trading days
following the crash-day close.

The one-year return is computed survivorship-safely by compounding total daily
returns over that window, so stock splits, dividends, and delisting or bankruptcy
terminal returns are all reflected in the outcome; a crashed stock that is delisted
within the year contributes its realized (often near-total) loss rather than
disappearing from the sample. Crashes occurring within one trading year of the data
edge cannot be observed for a full year and are censored rather than labeled. This
leaves $262{,}906$ observed one-year outcomes in the liquid study pool.

The distribution of these one-year returns is shown in Figure~\ref{fig:m0}. After a
crash, an investor earns money one year later only $46.9\%$ of the time---less than a
coin flip. The distribution is strongly right-skewed: the \emph{mean} one-year return
is positive ($+10.2\%$), but the \emph{median} is negative ($-4.6\%$), and more than
one crash in five loses at least half its value. In other words, ``buy the dip'' at
the one-year horizon is, on average, a losing proposition for the typical stock; the
positive mean is driven by a minority of large recoverers in the right tail, not by
the median outcome. This is precisely the gap that a predictive model must
exploit---separating that recovering minority from the majority that continues to
decline.

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{figures/m0_return_hist.pdf}
\caption{Distribution of one-year total returns after a crash, over $262{,}906$
observed events in the liquid study pool (survivorship-safe: splits, dividends, and
delisting/bankruptcy terminal returns included; crashes within a year of the data
edge censored). The median return (dashed, red) falls below break-even while the mean
(dashed, green) is positive, reflecting the right skew. Returns above $+200\%$
($2.9\%$ of events, up to $+3{,}800\%$) are collected in the final bin.}
\label{fig:m0}
\end{figure}

The picture is strongly horizon-dependent. At a shorter $60$-trading-day
(roughly three-month) window (Figure~\ref{fig:m0-60d}), the outcome is closer to a
coin flip: $49.7\%$ of crashes earn money, with a median essentially at break-even
and a mean of only $+3\%$. Extending the horizon to one year does not improve the
\emph{typical} outcome---it worsens it. As the holding period lengthens the
distribution fans out: the right tail stretches (winners compound, lifting the mean
to $+10.2\%$) while the bulk drifts left (the median falls from $0\%$ to $-4.6\%$). A
crash thus offers a near-even short-term gamble that, for the median stock,
deteriorates over the following year, even as a widening minority of large recoverers
pulls the average upward.

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{figures/m0_return_hist_60d.pdf}
\caption{Distribution of $60$-trading-day total returns after a crash ($267{,}113$
observed events; same survivorship-safe construction as Figure~\ref{fig:m0}). The
distribution is more symmetric and centered near break-even, with the median and mean
close to zero. Returns above $+100\%$ are collected in the final bin.}
\label{fig:m0-60d}
\end{figure}

Finally, the base rate is regime-dependent, which cautions against reading any single
number as timeless. Restricting to crashes from $2010$ onward
(Figure~\ref{fig:m0-2010}, $54{,}216$ events) shifts the one-year distribution
markedly relative to the full history: the median rises to roughly break-even
($-0.5\%$) and the mean climbs to $+21\%$, reflecting the sustained post-crisis bull
market over which recoveries were both more frequent and larger. The unconditional
answer to ``is a crash a buying opportunity?'' therefore depends heavily on the
prevailing market regime---a dependence that recurs when we later evaluate predictive
models across time periods. In every era, though, Model~0 remains the benchmark: the
value of a predictive model is whatever it adds on top of these unconditional rates.

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{figures/m0_return_hist_2010.pdf}
\caption{One-year total returns after a crash, restricted to crashes from $2010$
onward ($54{,}216$ observed events). Relative to the full-history sample
(Figure~\ref{fig:m0}), the median rises to near break-even and the mean to $+21\%$,
illustrating how strongly the base rate depends on the market regime.}
\label{fig:m0-2010}
\end{figure}

\section{Which Features Correlate with Recovery?}

Before building predictive models, we screen individual features for a univariate
association with recovery, again defined as earning money one year after the crash.
These are descriptive relationships---unconditional, uncontrolled for era, sector, or
one another---so they flag where signal may live rather than establish causation. We
begin with company size.

\paragraph{Market capitalization.} We measure size by the point-in-time market
capitalization at the event (the crash-day close times the latest known shares
outstanding), available for $78\%$ of observed events, and split the sample into four
size ranges. Figures~\ref{fig:mcap-micro}--\ref{fig:mcap-mega} show the one-year return
distribution within each range, using the same construction as the Model~0 histograms.
The distribution shifts steadily to the right as size increases: the median one-year
return rises from $-10\%$ for small-caps to $+7\%$ for mega-caps, the mean from $+8\%$
to $+12\%$, and the share of crashes that earn money from $44\%$ to $55\%$. Only among
the largest firms does the median crash actually recover to a profit within a year.

A plausible reading is that large, established firms are more likely to survive and
mean-revert, whereas smaller companies are both more likely to keep sliding and more
likely to be delisted---their distributions are wider on both sides but centered lower.
The effect is real but modest (an $11$-point spread in recovery rate across the size
range) and, being univariate, is confounded with market regime, sector, and price
level. As later sections show, size adds little once price behavior and market context
are modeled; its value here is as a first, interpretable marker that recovery odds are
not uniform across the cross-section.

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{figures/recovery_hist_mcap_micro.pdf}
\caption{One-year return distribution for micro-cap crashes ($<\$100$M; $64{,}687$
events). Median $-6\%$, mean $+8\%$; $45\%$ earn money.}
\label{fig:mcap-micro}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{figures/recovery_hist_mcap_small.pdf}
\caption{One-year return distribution for small-cap crashes ($\$100$M--$\$1$B;
$97{,}806$ events). Median $-10\%$, mean $+10\%$; $44\%$ earn money---the lowest
recovery rate of the four ranges.}
\label{fig:mcap-small}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{figures/recovery_hist_mcap_midlarge.pdf}
\caption{One-year return distribution for mid/large-cap crashes ($\$1$B--$\$10$B;
$36{,}348$ events). Median $-1\%$, mean $+13\%$; $50\%$ earn money.}
\label{fig:mcap-midlarge}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{figures/recovery_hist_mcap_mega.pdf}
\caption{One-year return distribution for mega-cap crashes ($>\$10$B; $4{,}934$
events). Median $+7\%$, mean $+12\%$; $55\%$ earn money---the only range whose median
crash recovers to a profit.}
\label{fig:mcap-mega}
\end{figure}

\paragraph{Crash severity.} We next ask whether the depth of the initial drop predicts
recovery, measuring severity by the crash-day return and splitting events into four
fixed bands from $-10\%$ down to worse than $-30\%$
(Figures~\ref{fig:sev-10}--\ref{fig:sev-30}). A common intuition holds that a larger
drop is a larger overreaction and should therefore rebound more strongly. The data do
not support this for the typical stock: as the crash deepens, the recovery rate
\emph{falls} (from $47\%$ at $10$--$15\%$ to $43\%$ beyond $30\%$) and the median
one-year return worsens monotonically (from $-4\%$ to $-11\%$). Deeper crashes, on
average, keep falling---consistent with a larger drop signaling worse news rather than
a bigger bargain.

The mean, however, tells a subtler story: it stays roughly flat ($+10$ to $+12\%$)
across all severity bands, even as the median deteriorates. The reason is visible in the
histograms: the deepest crashes have the widest distribution, with both a heavier left
mass and a fatter right tail. A severe crash is thus a higher-variance bet---more likely
to keep sliding, but with a larger payoff on the rare occasion it does rebound. Severity
shifts the whole distribution left while stretching its right tail, rather than lifting
recoveries uniformly. As with size, the effect is modest and univariate, and crash
magnitude adds little once price behavior and market context are modeled.

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{figures/recovery_hist_sev_10_15.pdf}
\caption{One-year return distribution after a $10$--$15\%$ crash ($194{,}397$ events).
Median $-4\%$, mean $+10\%$; $47\%$ earn money.}
\label{fig:sev-10}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{figures/recovery_hist_sev_15_20.pdf}
\caption{One-year return distribution after a $15$--$20\%$ crash ($42{,}920$ events).
Median $-6\%$, mean $+10\%$; $46\%$ earn money.}
\label{fig:sev-15}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{figures/recovery_hist_sev_20_30.pdf}
\caption{One-year return distribution after a $20$--$30\%$ crash ($20{,}020$ events).
Median $-7\%$, mean $+12\%$; $46\%$ earn money.}
\label{fig:sev-20}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{figures/recovery_hist_sev_30plus.pdf}
\caption{One-year return distribution after a crash worse than $30\%$ ($5{,}569$
events). Median $-11\%$, mean $+10\%$; only $43\%$ earn money, the lowest of the four
bands---yet the widest distribution, with the fattest right tail.}
\label{fig:sev-30}
\end{figure}

This severity effect is specific to the one-year horizon. At the shorter $60$-trading-day
window (Figures~\ref{fig:sev-10-60}--\ref{fig:sev-30-60}), crash depth carries almost no
signal: every band earns money close to half the time ($48.7$--$49.9\%$) with a median
essentially at break-even, and the distributions are nearly indistinguishable. The
``deeper crashes recover worse'' pattern is therefore a slow, long-horizon
phenomenon---it emerges only as the year unfolds and the hardest-hit companies keep
declining, echoing the way the return distribution fans out with horizon
(Section~\ref{sec:m0}).

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{figures/recovery_hist_sev_10_15_60d.pdf}
\caption{$60$-day return distribution after a $10$--$15\%$ crash. Median $0\%$, mean
$+3\%$; $50\%$ earn money.}
\label{fig:sev-10-60}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{figures/recovery_hist_sev_15_20_60d.pdf}
\caption{$60$-day return distribution after a $15$--$20\%$ crash. Median $0\%$, mean
$+3\%$; $49\%$ earn money.}
\label{fig:sev-15-60}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{figures/recovery_hist_sev_20_30_60d.pdf}
\caption{$60$-day return distribution after a $20$--$30\%$ crash. Median $0\%$, mean
$+4\%$; $50\%$ earn money.}
\label{fig:sev-20-60}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{figures/recovery_hist_sev_30plus_60d.pdf}
\caption{$60$-day return distribution after a crash worse than $30\%$. Median $-1\%$,
mean $+3\%$; $49\%$ earn money---barely different from milder crashes at this horizon.}
\label{fig:sev-30-60}
\end{figure}

\paragraph{Prior-crash history.} A stock's recent crash history is among the most
informative signals we screen, foreshadowing its prominence in the models. We classify each event by
how many crashes the security suffered in the preceding $20$ trading days---today's
crash never counts as its own prior---into three cohorts: \emph{fresh} (no prior crash;
$59\%$ of events), \emph{one prior} ($22\%$), and \emph{two or more prior} ($19\%$).
Figures~\ref{fig:prior-0}--\ref{fig:prior-2} show the one-year return distribution for
each. The relationship is the strongest we have seen: the median one-year return falls
from $-2\%$ for fresh crashes to $-7\%$ with one prior crash and $-14\%$ with two or
more, while the recovery rate drops from $49\%$ to $43\%$. Repeatedly crashing over a
few weeks is a strong signal of a company in genuine decline, and over a year those
names keep falling.

As with crash severity, the mean moves the opposite way to the median---rising slightly
to $+11\%$ for the repeat-crash cohort even as its median collapses---because the
distribution widens with each additional crash, developing the heaviest left mass and
the fattest right tail of any slice in this section. Repeat crashers are thus the
highest-variance bet: most continue to decline, but a minority rebound spectacularly.
One caution on horizon: this is the one-year picture, and the sign can invert over
shorter windows, where beaten-down, high-volatility names are unusually prone to a
near-term technical bounce; recent-crash history accordingly enters the predictive
models as a powerful but horizon-dependent signal.

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{figures/recovery_hist_prior_0.pdf}
\caption{One-year return distribution for fresh crashes (no prior crash in the previous
$20$ trading days; $155{,}125$ events). Median $-2\%$, mean $+10\%$; $49\%$ earn money.}
\label{fig:prior-0}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{figures/recovery_hist_prior_1.pdf}
\caption{One-year return distribution with exactly one prior crash in the previous $20$
trading days ($57{,}088$ events). Median $-7\%$, mean $+11\%$; $46\%$ earn money.}
\label{fig:prior-1}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{figures/recovery_hist_prior_2plus.pdf}
\caption{One-year return distribution with two or more prior crashes in the previous
$20$ trading days ($50{,}693$ events). Median $-14\%$, mean $+11\%$; only $43\%$ earn
money---the lowest recovery rate and widest distribution of any slice examined.}
\label{fig:prior-2}
\end{figure}

\paragraph{Valuation (P/E).} The features above are all derived from price and trading
behavior; valuation is our one fundamental screen. We compute each event's
price-to-earnings ratio at the crash---the crash-day market capitalization over
trailing-twelve-month net income---and group events into unprofitable firms (negative
earnings, hence no meaningful P/E) and three valuation tiers among profitable firms:
cheap ($0<\text{P/E}\le 15$), fair ($15<\text{P/E}\le 30$), and expensive
($\text{P/E}>30$). A P/E is available for $74\%$ of observed events;
Figures~\ref{fig:pe-unprofitable}--\ref{fig:pe-expensive} show the one-year return
distribution for each group.

This is the sharpest relationship in the section. Unprofitable companies fare
disastrously: a median one-year return of $-28\%$, only $35\%$ earning money, and a
\emph{negative} mean---the sole slice in this paper whose average crash loses money over
the following year. Among profitable firms, cheaper is dramatically better: cheap stocks
recover strongly ($61\%$ earn money, median $+16\%$, mean $+31\%$), fair-valued ones
modestly ($52\%$, median $+2\%$), and expensive ones poorly ($42\%$, median $-13\%$).
The spread---from $35\%$ to $61\%$ in recovery rate---is by far the widest we observe. A
crash in a cheap, profitable company looks like an overreaction that tends to reverse; a
crash in an unprofitable or richly-valued one looks like the start of a longer decline.

Two caveats temper this striking result. First, valuation is heavily confounded with
size and quality: unprofitable firms are disproportionately micro-caps, so this screen
partly re-expresses the size effect. Second, and importantly, the association is a
\emph{long-horizon} one. The predictive models developed later target a much shorter
($20$-trading-day) recovery, and over that window fundamentals---valuation
included---add little; value reversion after a crash is a slow process that plays out
over months, not weeks. Valuation thus looks powerful for the one-year question studied
here yet nearly irrelevant for short-term rebound prediction, a horizon distinction we
return to in the modeling results.

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{figures/recovery_hist_pe_unprofitable.pdf}
\caption{One-year return distribution for unprofitable crashers (negative trailing
earnings; $85{,}613$ events). Median $-28\%$, mean $-3\%$; only $35\%$ earn money---the
worst outcomes of any slice, and the only one with a negative mean.}
\label{fig:pe-unprofitable}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{figures/recovery_hist_pe_cheap.pdf}
\caption{One-year return distribution for cheap profitable crashers ($0<\text{P/E}\le
15$; $49{,}422$ events). Median $+16\%$, mean $+31\%$; $61\%$ earn money---the strongest
recovery of any slice.}
\label{fig:pe-cheap}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{figures/recovery_hist_pe_fair.pdf}
\caption{One-year return distribution for fair-valued crashers ($15<\text{P/E}\le 30$;
$30{,}067$ events). Median $+2\%$, mean $+14\%$; $52\%$ earn money.}
\label{fig:pe-fair}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{figures/recovery_hist_pe_expensive.pdf}
\caption{One-year return distribution for expensive crashers ($\text{P/E}>30$;
$28{,}759$ events). Median $-13\%$, mean $+5\%$; $42\%$ earn money.}
\label{fig:pe-expensive}
\end{figure}

\paragraph{Pre-crash momentum.} Bucketing by the stock's return over the year
\emph{before} the crash (Figures~\ref{fig:premom-dn}--\ref{fig:premom-hot}) reveals a
reversal effect: prior big winners fare worst. Stocks up more than $50\%$ going in
recover only $37\%$ of the time (median $-20\%$), while those that had drifted mildly
lower recover best ($55\%$, median $+7\%$). A crash that interrupts a strong run tends to
be the start of a de-rating.

\begin{figure}[H]\centering\includegraphics[width=0.8\textwidth]{figures/recovery_hist_premom_dn.pdf}
\caption{Pre-crash one-year return below $-50\%$ ($34{,}325$ events). Median $+1\%$; $50\%$ earn money.}\label{fig:premom-dn}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.8\textwidth]{figures/recovery_hist_premom_mild.pdf}
\caption{Pre-crash one-year return $-50$ to $0\%$ ($74{,}244$ events). Median $+7\%$; $55\%$ earn money---the best of the four.}\label{fig:premom-mild}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.8\textwidth]{figures/recovery_hist_premom_up.pdf}
\caption{Pre-crash one-year return $0$ to $+50\%$ ($38{,}646$ events). Median $+2\%$; $52\%$ earn money.}\label{fig:premom-up}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.8\textwidth]{figures/recovery_hist_premom_hot.pdf}
\caption{Pre-crash one-year return above $+50\%$ ($47{,}332$ events). Median $-20\%$; only $37\%$ earn money---prior winners fare worst.}\label{fig:premom-hot}\end{figure}

\paragraph{Beta.} Market beta over the prior year (Figures~\ref{fig:beta-lo}--\ref{fig:beta-vhi})
tells the same story through volatility. Very-high-beta stocks ($\beta>1.8$) recover only
$37\%$ of the time (median $-23\%$); market-like names ($\beta\approx1$) do best ($55\%$,
median $+6\%$). Beyond a point, higher systematic risk maps to worse, not better, recovery.

\begin{figure}[H]\centering\includegraphics[width=0.8\textwidth]{figures/recovery_hist_beta_lo.pdf}
\caption{$\beta<0.8$ (defensive; $59{,}947$ events). Median $+2\%$; $51\%$ earn money.}\label{fig:beta-lo}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.8\textwidth]{figures/recovery_hist_beta_mkt.pdf}
\caption{$\beta$ $0.8$--$1.2$ (market-like; $47{,}092$ events). Median $+6\%$; $55\%$ earn money.}\label{fig:beta-mkt}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.8\textwidth]{figures/recovery_hist_beta_hi.pdf}
\caption{$\beta$ $1.2$--$1.8$ (high; $67{,}983$ events). Median $+4\%$; $52\%$ earn money.}\label{fig:beta-hi}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.8\textwidth]{figures/recovery_hist_beta_vhi.pdf}
\caption{$\beta>1.8$ (very high; $67{,}814$ events). Median $-23\%$; only $37\%$ earn money.}\label{fig:beta-vhi}\end{figure}

\paragraph{Profitability (EBITDA margin).} Operating profitability produces the single
worst slice in the paper (Figures~\ref{fig:ebm-neg}--\ref{fig:ebm-hi}): companies with
negative EBITDA margins earn money only $30\%$ of the time after a crash, with a median
one-year return of $-38\%$. Recovery then rises monotonically with margin, to $57\%$
(median $+10\%$) above $25\%$. Losing money operationally makes a crash far more likely to
be terminal.

\begin{figure}[H]\centering\includegraphics[width=0.8\textwidth]{figures/recovery_hist_ebm_neg.pdf}
\caption{EBITDA margin $<0$ ($45{,}843$ events). Median $-38\%$; only $30\%$ earn money---the worst slice in the paper.}\label{fig:ebm-neg}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.8\textwidth]{figures/recovery_hist_ebm_lo.pdf}
\caption{EBITDA margin $0$--$10\%$ ($38{,}789$ events). Median $0\%$; $50\%$ earn money.}\label{fig:ebm-lo}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.8\textwidth]{figures/recovery_hist_ebm_mid.pdf}
\caption{EBITDA margin $10$--$25\%$ ($52{,}045$ events). Median $+4\%$; $53\%$ earn money.}\label{fig:ebm-mid}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.8\textwidth]{figures/recovery_hist_ebm_hi.pdf}
\caption{EBITDA margin $>25\%$ ($28{,}077$ events). Median $+10\%$; $57\%$ earn money.}\label{fig:ebm-hi}\end{figure}

\paragraph{Revenue growth.} Year-over-year revenue growth
(Figures~\ref{fig:revg-dn}--\ref{fig:revg-hi}) echoes valuation: hyper-growth firms
($>30\%$) recover worst ($36\%$, median $-25\%$), moderate growers ($0$--$10\%$) best
($58\%$, median $+11\%$). Together with the P/E and momentum results, this marks
expensive, high-expectation growth stocks as the most fragile after a crash.

\begin{figure}[H]\centering\includegraphics[width=0.8\textwidth]{figures/recovery_hist_revg_dn.pdf}
\caption{Revenue declining year-over-year ($52{,}281$ events). Median $-3\%$; $48\%$ earn money.}\label{fig:revg-dn}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.8\textwidth]{figures/recovery_hist_revg_lo.pdf}
\caption{Revenue growth $0$--$10\%$ ($26{,}743$ events). Median $+11\%$; $58\%$ earn money---the best.}\label{fig:revg-lo}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.8\textwidth]{figures/recovery_hist_revg_mid.pdf}
\caption{Revenue growth $10$--$30\%$ ($37{,}396$ events). Median $+5\%$; $54\%$ earn money.}\label{fig:revg-mid}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.8\textwidth]{figures/recovery_hist_revg_hi.pdf}
\caption{Revenue growth $>30\%$ ($67{,}818$ events). Median $-25\%$; only $36\%$ earn money.}\label{fig:revg-hi}\end{figure}

\paragraph{Market context.} The broad market's trailing $20$-day return around the crash
(Figures~\ref{fig:mkt-crash}--\ref{fig:mkt-up}) is one of the strongest signals here, and
it inverts the intuition: crashes that occur while the whole market is already down more
than $10\%$ recover best of all ($63\%$, median $+19\%$), while crashes struck in a calm
or rising market recover poorly ($\sim43\%$, median $\sim-10\%$). A stock swept down in a
market-wide rout is far more recoverable than one that fell on its own.

\begin{figure}[H]\centering\includegraphics[width=0.8\textwidth]{figures/recovery_hist_mkt_crash.pdf}
\caption{Market $20$-day return $<-10\%$ ($52{,}517$ events). Median $+19\%$; $63\%$ earn money---the strongest single bucket.}\label{fig:mkt-crash}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.8\textwidth]{figures/recovery_hist_mkt_dn.pdf}
\caption{Market $20$-day return $-10$ to $-3\%$ ($55{,}115$ events). Median $-12\%$; $43\%$ earn money.}\label{fig:mkt-dn}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.8\textwidth]{figures/recovery_hist_mkt_flat.pdf}
\caption{Market $20$-day return $-3$ to $0\%$ ($34{,}851$ events). Median $-11\%$; $43\%$ earn money.}\label{fig:mkt-flat}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.8\textwidth]{figures/recovery_hist_mkt_up.pdf}
\caption{Market $20$-day return $>0\%$ ($120{,}416$ events). Median $-10\%$; $43\%$ earn money.}\label{fig:mkt-up}\end{figure}

\paragraph{Performance relative to sector.} The same idea sharpens when we compare the
stock to its sector on the crash day (Figures~\ref{fig:relsec-worse}--\ref{fig:relsec-with}).
Stocks that fell far more than their sector---an idiosyncratic, company-specific
crash---recover only $44\%$ of the time (median $-9\%$); the rare stocks that fell roughly
\emph{with} or less than their sector recover $75\%$ of the time (median $+34\%$). Whether
a crash is company-specific or sector-wide is among the most discriminating facts about it,
and it foreshadows why market and sector context dominate the predictive models.

\begin{figure}[H]\centering\includegraphics[width=0.8\textwidth]{figures/recovery_hist_relsec_worse.pdf}
\caption{Crashed far worse than sector (stock minus sector $<-10\%$; $178{,}426$ events). Median $-9\%$; $44\%$ earn money.}\label{fig:relsec-worse}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.8\textwidth]{figures/recovery_hist_relsec_mid.pdf}
\caption{Crashed moderately worse than sector ($-10$ to $-3\%$; $76{,}451$ events). Median $+2\%$; $51\%$ earn money.}\label{fig:relsec-mid}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=0.8\textwidth]{figures/recovery_hist_relsec_with.pdf}
\caption{Crashed with or better than sector ($>-3\%$; $7{,}668$ events). Median $+34\%$; $75\%$ earn money---the clearest recovery signal in the section.}\label{fig:relsec-with}\end{figure}

\section{A One-Year Recovery Model}
\label{sec:model}

The descriptive screen showed that individual features tilt the one-year odds. A model
asks the sharper question: combined, do these features yield a \emph{calibrated} per-crash
probability of recovery? We keep the Model~0 target of Section~\ref{sec:m0}---the event
earns money if its survivorship-safe one-year total return from the crash-day close is
positive---and estimate $P(\text{earn money in a year})$ with gradient-boosted trees
(XGBoost), which absorb missing fundamentals natively and capture the nonlinear
interactions the histograms only hinted at.

\subsection{Organizing the data}

The honest test of a forecaster is a future it has not seen, so we split chronologically:
train on the distant past, validate on an intermediate period, and hold out the most recent
years for a single final test (Table~\ref{tab:splits}). Because each outcome spans $252$
trading days, a \emph{one-year embargo} brackets every boundary (the 2018 and 2021 crash-years
are dropped) so that no outcome window straddles two splits. The cost of honesty is visible in
the base rates: crashes in 2019--2020 recovered $78\%$ of the time---they were measured through
the 2020--21 rebound---while 2022+ crashes recovered only $36\%$. Recovery base rates are
strongly non-stationary, and a model trained on the long-run average walks into 2022 predicting
too high. This is a calibration problem, and we return to it below.

\begin{table}[H]
\centering
\begin{tabular}{lrrl}
\toprule
Split & Events & $P(\text{earn})$ & Span \\
\midrule
train      & 225{,}436 & 46.5\% & 1926--2017 \\
validation &  14{,}988 & 78.1\% & 2019--2020 \\
test       &  13{,}635 & 35.7\% & 2022--2025 \\
\bottomrule
\end{tabular}
\caption{Chronological split with a one-year embargo (the 2018 and 2021 crash-years fall in
the embargo). The periods live in different worlds---a $78\%$ vs.\ $36\%$ base rate---which is
the non-stationarity the model must contend with.}
\label{tab:splits}
\end{table}

\subsection{What the model sees}

The model is given the ten features from the feature-correlation section---log market cap,
crash severity, prior-crash count (20d), P/E, pre-crash one-year return, 252-day beta, EBITDA
margin, revenue growth, market 20-day return, and crash-day return relative to sector. To let
it recognize the \emph{environment} a stock is crashing into---the direct remedy for the
non-stationary base rate above---we optionally add five point-in-time market-regime features,
all trailing (hence knowable at the crash-day close): the market's trailing 126-day and
252-day return, its drawdown from the trailing 52-week high, its trailing 60-day realized
volatility, and \emph{crash breadth} (the fraction of the universe crashing $\ge$$10\%$ in a
day, averaged over the trailing 20 days). Hyperparameters are chosen on a predeclared grid
scored on the validation period only, with early stopping on validation; the test set is
touched exactly once.

\subsection{Results}

Table~\ref{tab:model-results} reports the single held-out test, against a Model~0 baseline
that predicts the constant training base rate for every crash.

\begin{table}[H]
\centering
\begin{tabular}{lrrrrr}
\toprule
Model & Log loss & Brier & ROC-AUC & PR-AUC & ECE \\
\midrule
\multicolumn{6}{l}{\emph{Chronological test (2022+, $n=13{,}635$, actual $P(\text{earn})=35.7\%$)}}\\
\quad Model 0 (base rate)            & 0.6757 & 0.2413 & --- & 0.357 & --- \\
\quad XGBoost, 10 features           & 0.6645 & 0.2352 & 0.592 & 0.428 & 0.093 \\
\quad XGBoost, 15 features (+regime) & \textbf{0.6540} & \textbf{0.2307} & 0.589 & 0.428 & \textbf{0.061} \\
\bottomrule
\end{tabular}
\caption{Held-out test performance. Lower is better for log loss, Brier, and ECE (expected
calibration error); higher is better for the AUCs. The regime features move the model's
average prediction from $0.42$ toward the true $0.36$.}
\label{tab:model-results}
\end{table}

Figure~\ref{fig:vs-m0} makes the comparison visual. Sorting the test crashes into ten
equal-size buckets by predicted probability, each bar is a bucket's \emph{actual} recovery
rate; Model~0's single constant forecast is the flat line. Model~0 must assign every crash the
same probability, so its only prediction is that line; a useful model spreads crashes into
buckets whose realized recovery rates fan out around it. The gap between the flat line and the
rising staircase is exactly the discrimination the metrics summarize---slim but monotone,
from a bottom-decile recovery rate of $24\%$ to $48\%$ in the top decile.

\begin{figure}[H]
\centering
\includegraphics[width=0.62\textwidth]{figures/model_vs_m0.pdf}
\caption{Model vs.\ Model~0 on the honest out-of-time test. Bars: actual recovery rate per
predicted-probability decile (lowest to highest) of the 15-feature model. Dashed line:
Model~0's constant base-rate forecast, identical for every crash.}
\label{fig:vs-m0}
\end{figure}

\paragraph{Discrimination is modest but real out-of-time.} On the honest chronological test
the model beats the base rate on every proper metric (log loss, Brier, PR-AUC) with a
ROC-AUC of $0.59$---a genuine but slim edge, in line with what a hard forecasting problem
should yield.

\paragraph{Regime features fix the level, not the ranking.} Adding the five regime features
cuts calibration error by a third (ECE $0.093\!\to\!0.061$) and improves both proper scores,
while ROC-AUC and PR-AUC barely move. That is the signature of a feature that tells the model
\emph{what level} to predict rather than \emph{how to rank}: able at last to see it was
crashing into a risk-off tape, the model dials its average forecast down toward the truth.
Figure~\ref{fig:reliability} shows the reliability curve pulled toward the diagonal. The fix
is only partial---the mid-to-high buckets still over-predict---because 2022 was in part a
genuinely novel regime (a rate-shock grind more pessimistic than almost anything in the
1926--2017 training history). Regime features let the model recognize regimes it has seen;
they cannot conjure a base rate for one it has not.

\begin{figure}[H]
\centering
\includegraphics[width=0.62\textwidth]{figures/model_reliability.pdf}
\caption{Reliability of the chronological model on the 2022+ test set: predicted probability
vs.\ realized recovery rate, in ten equal-width bins. The regime features (blue) pull the
curve toward the $45^\circ$ perfect-calibration line relative to the price-and-fundamentals
model (grey).}
\label{fig:reliability}
\end{figure}

\paragraph{What the model leans on.} Figure~\ref{fig:importance} ranks the fifteen features
by gain. Market-regime features and beta take the top slots (trailing volatility, 252-day
market return, crash breadth), followed by the fundamentals (P/E, revenue growth, EBITDA
margin); the crash's own depth and the stock's prior-crash count are near-noise. This echoes
the descriptive finding that \emph{context}---where the market and sector are---separates
one-year recoveries more sharply than the mechanics of the individual drop.

\begin{figure}[H]
\centering
\includegraphics[width=0.78\textwidth]{figures/model_importance.pdf}
\caption{Feature importance (fraction of total gain) for the 15-feature chronological model.
Market-regime features (green) dominate the top of the ranking; the short-horizon market
return the model previously relied on fades once the longer-horizon regime signals are
available.}
\label{fig:importance}
\end{figure}

\end{document}
